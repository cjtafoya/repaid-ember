import { balanceMath } from 'fair-share/helpers/balance-math';
import { module, test } from 'qunit';

module('Unit | Helper | balance math');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = balanceMath([42]);
  assert.ok(result);
});
